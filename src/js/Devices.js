class devices {
    constructor(sensorType, name, platform, board, update_interval, icon) {
        this.sensorType = sensorType;
        this.name = name;
        this.platform = platform;
        this.board = board;
        this.update_interval = update_interval;
        this.icon = icon;
    }

}